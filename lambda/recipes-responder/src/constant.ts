//todo - as of now creating for recipes-responder but we can check if we need constant file globally means at root of the project to hold all constants?)

export const INDEX_JSON = "index.json"
export const V2_INDEX_JSON = "v2/index.json"
